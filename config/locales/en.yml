# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  users:
    roles:
      user: User
      admin: Administrator
    index:
      confirm:
        log_delete: Are you sure you want to remove this log for this user?
      caption:
        name: Name
        email: Email
        role: Role
        actions: Actions
    new:
      submit: Create
      title: Create a User
    fields:
      first_name: First name
      last_name: Last name
      email: Email
      password: Password
      password_confirmation: Re-enter password
    user:
      confirm: "Are you sure you want to remove access to this list from this user?"
      access: "Access to:"
      delete: Delete
  sessions:
    fields:
      email: Email
      password: Password
      remember_me: Remember me
    new:
      title: Login
      submit: Login
  shared:
    navigation:
      logs: Logs
      users: Users
      account: Account
      logout: Logout
  logs:
    index:
      caption:
        name: Name
        mtime: Last Modified
    navigation:
      list: List
      new: Add a log
    new:
      submit: Create
    fields:
      name: Name
      log_path: Path to Log File
      root_path: Document Root
      root_path_intro: This is the path the code files exist at on your local computer. If this is not set right the TextMate links in backtraces will not work.
      log_type: Log format
      